@model IEnumerable<CinemaMasters.Models.Reservierung>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kinobesucher.Name)
        </th>
        <th>
            Kinosaal
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vorstellung.Zeit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vorstellung.Film)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kinobesucher.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vorstellung.Kinosaal.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vorstellung.Zeit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vorstellung.Film.Titel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
